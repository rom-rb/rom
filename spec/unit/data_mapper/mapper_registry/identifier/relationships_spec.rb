require 'spec_helper'

describe MapperRegistry::Identifier, '#relationships' do
  it 'needs unit specs' do
    pending('DataMapper::MapperRegistry::Identifier#relationships unit specs are not implemented yet')
  end
end

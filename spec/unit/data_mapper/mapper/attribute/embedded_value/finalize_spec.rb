require 'spec_helper'

describe Mapper::Attribute::EmbeddedValue, '#finalize' do
  it 'needs unit specs' do
    pending('DataMapper::Mapper::Attribute::EmbeddedValue#finalize unit specs are not implemented yet')
  end
end
